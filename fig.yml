app:
  build: .
  environment:
    RAILS_ENV:
  ports:
    - "3000:3000"
  links:
    - db
db:
  image: postgres:9.3
  environment:
    POSTGRESQL_USER: postgres
    POSTGRESQL_PASS:
    POSTGRESQL_DB: postgres
